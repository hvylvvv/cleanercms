name: cleanercms
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  fl_chart: ^0.68.0
  flutter_svg: ^2.0.5

  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1 # If you plan to use Firebase Authentication
  cloud_firestore: ^5.0.2
  url_launcher: ^6.0.20
  csv: ^5.0.0
  path_provider: ^2.0.12

  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  location_picker_flutter_map: ^3.0.1
  flutter_map_cancellable_tile_provider: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/  
